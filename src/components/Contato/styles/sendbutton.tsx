import styled from "styled-components";
import { CallToAction } from "../../commons/callToAction";

export const SendButton = styled(CallToAction)`
  margin: 0;
  margin-top: 32px;
  padding: 16px 52px;
`;
